<mvc:View xmlns:mvc="sap.ui.core.mvc" xmlns="sap.m">
    <Table items="{viewModel>/to_Version}">
        <headerToolbar>
            <Toolbar>
                <Title text="Version(s)"/>
            </Toolbar>
        </headerToolbar>
        <columns>
            <Column>
                <Text text="VersionId"/>
            </Column>
            <Column>
                <Text text="Status"/>
            </Column>
            <Column>
                <Text text="Title"/>
            </Column>
            <Column>
                <Text text="Description"/>
            </Column>
        </columns>
        <items>
            <ColumnListItem>
                <cells>
                    <Text text="{viewModel>TarMapId}"/>
                    <Text text="{viewModel>Status}"/>
                    <Text text="{viewModel>Title}"/>
                    <Text text="{viewModel>Description}"/>
                </cells>
            </ColumnListItem>
        </items>
    </Table>
</mvc:View>