<mvc:View 
    xmlns:mvc="sap.ui.core.mvc" 
    xmlns="sap.m" 
    xmlns:f="sap.ui.layout.form" 
    xmlns:core="sap.ui.core"
    controllerName="fiorilibappname.controller.Service">
    <Table items="{singleSolutionModel>/to_Service}" visible="{= !${viewSettings>/isEditMode}? true : false}">
        <headerToolbar>
            <Toolbar>
            </Toolbar>
        </headerToolbar>
        <columns>
            <Column>
                <Text text="Service ID"/>
            </Column>
            <Column>
                <Text text="Description"/>
            </Column>
            <Column>
                <Text text="Version"/>
            </Column>
            <Column>
                <Text text="Url"/>
            </Column>
            <Column>
                <Text text="ExtServName"/>
            </Column>
            <Column>
                <Text text="SoftCompVersion"/>
            </Column>
            <Column>
                <Text text="BeAuthRole"/>
            </Column>
        </columns>
        <items>
            <ColumnListItem>
                <cells>
                    <Text text="{singleSolutionModel>ServiceId}"/>
                    <Text text="{singleSolutionModel>Description}"/>
                    <Text text="{singleSolutionModel>Version}"/>
                    <Link text="{singleSolutionModel>Url}" href="{singleSolutionModel>Url}" target="_blank"/>
                    <Text text="{singleSolutionModel>ExtServName}"/>
                    <Text text="{singleSolutionModel>SoftCompVersion}"/>
                    <Text text="{singleSolutionModel>BeAuthRole}"/>
                </cells>
            </ColumnListItem>
        </items>
    </Table>
    <Table id="servicesTable" items="{detailView>/to_Service}" visible="{= ${viewSettings>/isEditMode}? true : false}" mode="MultiSelect">
        <headerToolbar>
            <Toolbar>
                <Title text="Odata Service(s)"/>
                <ToolbarSpacer/>
                <Button text="Add" icon="sap-icon://add" press="onAddService" visible="{= ${viewSettings>/isEditMode}? true : false}"/>
                <Button text="Delete" icon="sap-icon://delete" press="onDeleteService" visible="{= ${viewSettings>/isEditMode}? true : false}"/>
            </Toolbar>
        </headerToolbar>
        <columns>
            <Column>
                <Text text="Service ID"/>
            </Column>
            <Column>
                <Text text="Description"/>
            </Column>
            <Column>
                <Text text="Version"/>
            </Column>
            <Column>
                <Text text="Url"/>
            </Column>
            <Column>
                <Text text="ExtServName"/>
            </Column>
            <Column>
                <Text text="SoftCompVersion"/>
            </Column>
            <Column>
                <Text text="BeAuthRole"/>
            </Column>
        </columns>
        <items>
            <ColumnListItem>
                <cells>
                    <Input value="{detailView>ServiceId}" editable="false"/>
                    <Input value="{detailView>Description}" editable="{= ${viewSettings>/isEditMode}? true : false}"/>
                    <ComboBox selectedKey="{detailView>Version}" editable="{= ${viewSettings>/isEditMode}? true : false}">
                        <core:Item key="1.0" text="1.0"/>
                        <core:Item key="2.0" text="2.0"/>
                        <core:Item key="4.0" text="4.0"/>
                    </ComboBox>
                    <Input value="{detailView>Url}" editable="{= ${viewSettings>/isEditMode}? true : false}"/>
                    <Input value="{detailView>ExtServName}" editable="{= ${viewSettings>/isEditMode}? true : false}"/>
                    <Input value="{detailView>SoftCompVersion}" editable="{= ${viewSettings>/isEditMode}? true : false}"/>
                    <Input value="{detailView>BeAuthRole}" editable="{= ${viewSettings>/isEditMode}? true : false}"/>
                </cells>
            </ColumnListItem>
        </items>
    </Table>
</mvc:View>